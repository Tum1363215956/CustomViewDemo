<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.tum.customview">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity" />
        <activity android:name=".animation.acitivity.PointEvaluatorActivity" />
        <activity android:name=".listview.ListViewDemo1" />
        <activity android:name=".listview.downdraglistview.DownDragLayout" />
        <activity android:name=".draggridview.DragActivity"></activity>
        <activity android:name=".popwindow.PopActivity" />
        <activity android:name=".meituan.MeiTuanActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".percentlayout.PercentLayoutActivity"></activity>
    </application>

</manifest>